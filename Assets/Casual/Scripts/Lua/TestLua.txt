local U = CS.UnityEngine
local log = U.Debug.Log

function start()
	log("start " .. self.gameObject.name)
end

function update()
	self.transform:Rotate(0, U.Time.deltaTime * 90, 0)
end

function ondestroy()
	log("ondestroy")
end
